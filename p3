#!/bin/bash
# P3 Smart Launcher - Finds p3.py in current directory tree

dir="$PWD"
while [[ "$dir" != "/" ]]; do
    if [[ -f "$dir/p3.py" ]]; then
        cd "$dir" && exec python p3.py "$@"
    fi
    dir=$(dirname "$dir")
done

echo "❌ p3.py not found in current directory tree" >&2
exit 1